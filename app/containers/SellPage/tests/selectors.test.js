// import { fromJS } from 'immutable';
// import { makeSelectSellPageDomain } from '../selectors';

// const selector = makeSelectSellPageDomain();

describe('makeSelectSellPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
